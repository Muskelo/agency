import {defineStore} from 'pinia'

export const useCurrentUser = defineStore("currentUser", {
    state: () => {
        return {
        }
    },
    actions: {
        async login_m(){
            
        }
    }

})
